# P5 — 찍기방지 (Anti-Guessing) 상세 설계 · 구현

작성: 2026-07-21 · Genius Project (psle-app) · 대상: 중국어 60세트 일일연습 + 영어

방식: **B(행동기반 감지) + C(변별 확인) 하이브리드**, 단 **최우선 원칙은 "제대로 푼 학생은 아무것도 느끼지 않는다"**.

---

## 0. 잠긴 원칙 (product decision)

> 정상적으로 실력으로 푼 학생에게는 **추가 단계 0, 마찰 0, 잔소리 0**.
> 오직 **아이가 물리적으로 읽을 수도 없는 속도**로 찍은 명백한 경우만 잡는다.
> 애매하면 **잡지 않는다**(오판보다 미탐이 낫다). 점수는 절대 깎지 않는다.
> 찍은 정답은 벌이 아니라, 요행을 '숙달'로 오인하지 않도록 **복습으로 회수**할 뿐.

이 원칙이 다른 모든 정교함보다 우선한다. 학습의욕 저하·문제풀이 짜증을 만드는 순간 실패다.

---

## 1. 왜 "최소 기능"인가

찍기 감지는 태생적으로 오판 위험이 있다. 빠르게 맞힌 학생 = 찍은 학생이 아니다 — 아는 학생은 원래 빠르다. 그래서 흔한 설계(응답시간 4초 미만=찍기 등)는 **성실한 학생을 계속 의심**해서 의욕을 꺾는다. 실제로 영어 엔진의 기존 `guessFlag`는 4~8초 임계라, 단어를 아는 아이의 정상 답까지 "⚡ Quick Answer Flagged"로 표시하고 있었다. 이건 정확히 우리가 피해야 할 것이다.

그래서 P5는 **판정 감도를 극단적으로 낮춰**, "읽는 것 자체가 불가능한 속도"만 잡는다.

---

## 2. 감지 (B) — 극보수 단일 신호

문항 유형에 따라 두 가지 관측만 쓴다. 둘 다 오판이 거의 불가능한 신호다.

### 2-1. 중국어(ExamSession) — 연타 감지 (burst-tap)
중국어 MCQ는 한 페이지에 여러 문항이 한꺼번에 보이므로 **문항별 읽기시간을 관측할 수 없다.** 대신 **연속 답 탭 사이의 간격**을 본다. 진짜 푸는 학생은 다음 문항의 지문·보기를 읽고 나서 누르므로 초 단위가 걸린다. 오직 마구 누르는 경우만 0.x초 간격이 나온다.

- 판정: **정답**인 문항이 직전 답 탭과 **0.7초 미만** 간격으로 눌렸을 때(첫 답은 페이지 로드 후 0.6초 미만일 때).
- 오답은 어차피 복습으로 가므로 대상 아님(`onlyCorrect`).
- 단일 문항 페이지는 대상 아님(간격 없음).
- 위치편향·오답후정답 등 오판 위험 신호는 **일부러 넣지 않음**(최소 기능).
- 구현: `src/lib/antiGuess.js`의 `detectPageGuesses`. 임계값은 `GUESS_CFG`에서 한 곳으로 조절.

### 2-2. 영어(EnglishSession) — 불가능 속도 하한
영어 MCQ는 **한 문항씩** 뜨고 "선택→확인" 2탭 구조라 **진짜 문항별 시간**이 잡힌다. 여기선 "읽는 것이 불가능한 하한"만 잡는다.

- 임계(`sessionUtils.guessFlag`, ms): MCQ 1500 · Cloze 1800 · Editing 1500 · Comprehension 2500 · 기본 1500.
- 기존 4000~8000 → 위 값으로 **대폭 하향**. 정상 속독 학생은 이제 걸리지 않는다.

두 엔진 모두 "물리적으로 불가능한 속도"라는 **동일 철학**으로 정렬된다(값만 매체 특성에 맞춤).

---

## 3. 처리 (C) — 흐름을 끊지 않는 변별 확인

찍기로 판정돼도 **문제 푸는 중엔 아무 팝업도 뜨지 않는다.** C(변별 확인)는 별도 인라인 단계 대신 **기존 자산으로 실현**해 마찰을 0으로 만든다.

- **점수 불변**: 정답은 정답으로 기록(`correct:true` 유지). 절대 감점 없음.
- **중국어**: 판정 문항의 핵심어를 조용히 **P4 복습관문으로 회수**(`zhAddUnknown`). 회차 끝 관문에서 인식 확인 = 변별 확인이 자연히 일어남. 요행이면 관문에서 못 맞히고 다음 회차 재출제. → 흐름 방해 0.
- **영어**: 판정은 결과 화면의 기존 "⚡ Quick Answer" 박스로 **투명하게** 보여줌(감지가 극보수라 이제 거의 안 뜸·뜨면 정확). 영어에는 P4식 어휘관문이 없어 회수는 표시까지가 최소 버전.
- **비침습 안내(중국어)**: 판정 문항에만, 채점 공개 후 **행동 요구 없는** 한 줄 칩("选得很快！这个词最后再确认一次 ✓"). 탭 불필요, 부정적 뉘앙스 없음.

정상 학생 경험: **바뀌는 것이 하나도 없음.**

---

## 4. 오판 최소화 튜닝 노브

전부 한 곳에서 더 보수적으로 내릴 수 있다.

- 중국어: `antiGuess.js` `GUESS_CFG.burstMs`(↓), `firstTapFloorMs`(↓), `onlyCorrect:true` 유지.
- 영어: `sessionUtils.js` `guessFlag`의 유형별 하한(↓).
- 초기 운영은 이 값으로 시작하고, 실제 로그에서 오판이 보이면 **더 내린다**(never up first).

---

## 5. 적용 범위

- **중국어 일일연습 60세트 전량** — MCQ 섹션(HanziMcq/PinyinMcq/VocabMcq)에 자동 적용(엔진 레벨). 세트별 저작 0.
- **영어** — MCQ 계열 per-item 판정.
- **미적용/후속 옵션**: 중국어 빈칸(词库 선택)·독해 MCQ, 영어 요행-정답의 오답노트 회수는 **저작·회귀 위험을 고려해 2차 확장 후보**로 둠(원칙은 동일 재사용 가능).
- **복습 재풀이(reviewMode)·모의고사 엔진**은 대상 아님.

---

## 6. 향후 확장 규칙 (승계)

1. 감지는 **오판보다 미탐**. 새 신호 추가는 오판율 검증 후에만.
2. 판정 시 **점수 불변·흐름 무방해**. 개입은 복습 회수/투명 표시까지.
3. 정상 학생 경험 불변이 **회귀 테스트의 1순위**.
4. 임계는 매체별로 "읽기 불가능 속도" 기준. 학년·과목 확장 시 그 매체의 최소 읽기시간으로 재산정.
