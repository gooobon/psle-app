# 중국어 단어 팝업·모르는단어 관문 — 업그레이드 설계서 (v1)

작성: 2026-07-21 · Genius Project (psle-app) · 보고·소통 한국어.
판단 기준은 하나 — **이게 학생의 실제 시험 성적을 올리는가.**
전제 정책: 단어 뜻은 **(B) 문맥별 뜻 유지 + 정확성 보장**(사용자 확정).

---

## 0. 한 줄 요약

> 학생이 중국어 문제를 풀다 **모르는 단어를 탭하면** 발음·뜻·동의어·반대어·예문 팝업이 뜨고,
> 한 번이라도 탭한(=몰랐던) 단어는 **회차 끝에 반드시 다시 확인하고 넘어가는 관문**으로 모아,
> "뜻 모르고 찍고 넘어가기"를 구조적으로 막는다.

---

## 1. 실측 규모 (추측 아님 · 전 60세트 전수)

학생이 화면에서 읽는 **자극 텍스트**(보기·문제·지문·어휘함·조합타일·독해)만 뽑아 분사(jieba) 실측:

| 항목 | 실측 |
|---|---|
| 자극 문자열(유니크) | 4,466개 |
| 누적 한자 | 74,237자 |
| 유니크 한자 | 1,611자 |
| **분사된 유니크 단어 토큰** | **3,936개** (다자어 3,007 + 단자어 929) |
| 그중 기존 keywords로 커버됨 | 1,026개 |
| **신규 사전 항목 필요** | **≈ 2,910개** |

→ "모든 단어 클릭 가능"의 실제 사전 규모는 **약 3,900 표제어**. 이 중 1,142개는 이미 사람이 만든 keywords(뜻·병음)가 있어 **신뢰 시드**로 재사용. 나머지 ~2,900개가 신규 저작 대상이다. 정직하게 말하면 이 콘텐츠 저작이 이 프로젝트 전체에서 가장 큰 덩어리다.

빈도 상위는 대부분 기능어/대명사(的·了·他·很·把·同学·妈妈…). 이건 "모르는 단어 관문"에 넣으면 오히려 방해되므로 별도 처리(§7·불용어).

---

## 2. 이미 앱이 가진 것 (새로 만들지 말고 확장할 자산)

실제 코드 확인 결과, 바닥부터 만들 필요 없다:

- **진도 구조**(`src/app/ClaudeApp.js`): `genius_progress_v2` = `{ history[], mistakes[], nextSession, sessionSections, vocabBook }`. 여기 **`unknownWords`·`vocabMastery`만 추가**하면 됨. `mistakes` 기반 리메디에이션 드릴(`buildRemediationDrill`)도 이미 있음 → 단어 복습에 재활용 가능.
- **탭 상호작용 선례**(`ExamSession.js`): 독해에 이미 "Tap to highlight in the passage"가 있음 → 지문/문장 span 탭은 검증된 패턴.
- **다음자(多音字) 데이터**: `duoyinzi_cursor.json` + `build_duoyinzi.js` 존재 → 문맥별 병음의 기반 자료 있음.
- **문맥별 뜻 시드**: 기존 `keywords`(1,573쌍)는 이미 문맥에서 고른 뜻 → **정책 B의 "문맥 뜻"을 그대로 잇는 출발점**.

---

## 3. 핵심 설계 결정

### 3-1. 분사는 **빌드타임**에 (런타임 jieba 금지)
브라우저에서 매번 분사하면 무겁고 결과가 비결정적이라 감사 불가. **빌드 때 한 번 분사**해 각 자극 문자열을 토큰 스팬 목록 `[{w, start, len, key}]`으로 미리 굳혀 `.ts`에 실어보낸다. 오분사(예: 고유명사 小明)는 **커스텀 사전 + 세트별 고유명사 목록**으로 교정하고, 최후엔 단자 폴백 → **어떤 글자도 클릭 불가로 남지 않음**(커버리지 100% 보장).

### 3-2. 사전은 **2계층** (정책 B의 핵심)
- **베이스 사전**(단어 1개 = 항목 1개, ~3,900): 그 단어의 P3 대표 뜻 + 병음 + 동의어/반대어 + 예문.
- **문맥 오버라이드**(해당 문항에서만): 그 문항 keywords에 문맥 뜻이 있으면 팝업이 **그 문맥 뜻을 우선 표시**. `带`가 A세트에선 "bring", B세트에선 "lead"로 뜨는 게 정책 B. 오버라이드 없으면 베이스 대표 뜻.

### 3-3. 병음 다음자 = 문맥 우선
표제어 병음은 pypinyin 자동 + 기존 keywords/duoyinzi로 교정. 다음자(着·长·数…)는 **문항 문맥 병음**을 오버라이드로 우선. §커버리지에서 잡은 병음 오류(着急 zhāojí→zháojí, 老实 lǎoshí→lǎoshi)도 여기서 정본화.

### 3-4. localStorage엔 **학생 상태만**
사전(정적 콘텐츠 ~3,900항목)은 **번들/.ts에 실림**(localStorage 아님). localStorage엔 학생이 탭한 단어·마스터리 상태(작음)만. 용량·성능 안전.

---

## 4. 사전 스키마 (신규 `zh_dict.json` → emit로 `.ts`)

```json
"愿望": {
  "py": "yuànwàng",
  "pos": "n",
  "en": "a wish; something you hope for",
  "syn": ["心愿", "梦想"],
  "ant": [],
  "ex": { "zh": "我的愿望是当一名医生。",
          "py": "wǒ de yuànwàng shì dāng yì míng yīshēng.",
          "en": "My wish is to become a doctor." }
}
```
- 동의어/반대어는 **P3 수준 실단어만**(교과서 밖 고급어 금지), 반대어 없는 단어는 `[]`.
- 예문은 **표제어를 반드시 포함**, 12자 내외, P3 문형.
- 문맥 오버라이드는 별도 맵 `overrides[itemId][word] = { en, py }`.

---

## 5. UI/UX — 클릭 팝업

- **`ClickableZh` 컴포넌트**: 문장/지문/보기를 미리 굳힌 토큰 스팬으로 렌더, 각 단어 탭 가능. §3-1 스팬을 §2의 기존 렌더 지점(문장 line 361, 지문 line 1271 등)에 끼움.
- **어포던스**: 클릭 가능 단어에 **은은한 점선 밑줄**(중국어 자형과 안 겹치게 오프셋, 기존 U_MARK 방식 재사용) + 첫 문항에 "모르는 단어를 탭하세요" 1회 코치마크. **안 보이면 학생은 기능을 못 찾는다** — 이게 성패를 가름.
- **팝업 내용**: 표제어·병음·(문맥)뜻·동의어·반대어·예문. 탭 밖 클릭 시 닫힘. **모바일 우선**(세션이 폰에서도 돌아감) — 하단 시트형 권장.
- 탭 = **조용히 기록**(마찰 0). "몰랐다" 신호로 수집(§7).

---

## 6. 모르는 단어 수집 + 회차말 복습 관문

1. 세션 중 탭한 단어 → `sessionSections`에 `unknownWords[]` 누적(불용어 제외).
2. **회차(문제세트) 전체 종료 시 = 복습 관문**: 모은 단어를 카드로 제시. 학생이 **능동 확인**해야 회차 완료·최종 점수 확정.
3. 사용자 요구대로 "**꼭 알고 넘어가도록**" — 관문 통과 전엔 세션이 완료로 저장되지 않음(soft-block).

---

## 7. 내 추가 개선안 (찍고 넘어가기 방지 — 사용자가 요청한 파트)

단순 "탭→뜻보기"에 더해, **대충 찍는 행동 자체를 겨냥**한 장치들:

1. **복습 관문을 재인(再認) 미니퀴즈로** — 그냥 "확인" 버튼이 아니라, 단어만 보여주고 **뜻을 가린 뒤 학생이 떠올리게** → 탭해서 정답 확인 → 스스로 "알아요 / 헷갈려요" 평가. 수동 클릭보다 학습 효과 큼.
2. **누적 단어장 + 간격 반복** — "헷갈려요"로 표시된 단어는 `vocabMastery`에 남아 **다음 회차 관문에 다시 등장**. 한 단어는 **≥2회 확인돼야 "마스터"**로 졸업(한 번 보고 잊는 것 방지).
3. **오답 순간 = 단어 학습 순간(soft nudge)** — MCQ를 틀렸는데 그 문항에서 **단어를 한 번도 안 탭했으면**, "핵심 단어를 확인해볼까요?" 하고 그 문항 keywords(핵심어)를 살짝 강조. 찍고 틀린 학생을 그 자리에서 붙잡음. (매 문항 강제 질문은 과함 → 배제.)
4. **핵심어 표시** — 각 문항은 이미 keywords(그 문제의 결정적 단어)를 가짐. 이걸 은은히 표시해 막힌 학생의 시선을 그리로. 오답 시 핵심어 뜻 자동 노출(기존 3층 해설 철학과 일치).
5. **오답↔단어 진단 리포트** — 회차 끝에 "틀린 5문제 중 4개는 이 단어들을 몰라서였어" 식으로 오답과 미확인 핵심어를 연결. 동기부여 + 진단.
6. **대시보드 지표** — "이번 회차 새로 배운 단어 X · 복습 대기 Y · 마스터 누계 Z". 부모/학생이 어휘 성장 체감.

---

## 8. 정확성 보장 (정책 B + 기계검증)

이 프로젝트가 굴러온 이유 = `merge_patch.py`류 **기계검증**. 사전도 같은 방식으로 강제:

신규 검증기 `build_zh_dict.py`의 **병합 거부 조건**:
1. **커버리지 강제** — 모든 자극 토큰이 사전 표제어(또는 단자 폴백)에 매핑. 미매핑 1건도 있으면 거부.
2. **필드 완결** — py·en 필수, 병음 성조기호(숫자·무성조 거부).
3. **예문 무결** — 예문에 표제어 포함, 예문 병음 성조기호.
4. **P3 어휘 상한** — 동의어/반대어가 사전 표제어집 안이거나 P3 화이트리스트 내(고급어 유입 차단).
5. **문맥 오버라이드 정합** — override의 word가 실제 그 itemId 자극에 존재.
6. 기존 검증(중복 뜻 등) 승계.

신뢰 시드: 기존 1,142개 keyword 뜻은 사람 검토분 → 우선 채택. 신규분은 검증 통과 후 **빈도 상위/세트별로 사람 스팟감사**(전량 육안은 비현실적이니 표본).

---

## 9. 구현 단계 (기존 리듬대로 · 검증 게이트)

| 단계 | 내용 | 산출 |
|---|---|---|
| **P1 골격** | 빌드타임 분사 + 스팬맵 + 사전 골격(자동: jieba+pypinyin+시드) + 커버리지 100% 기계검증 | `zh_dict.json`(초안), `zh_segments.ts`, `build_zh_dict.py` |
| **P2 콘텐츠** | 뜻·동의어·반대어·예문 저작(정책 B), 검증 게이트, 빈도/세트 배치, 스팟감사 | 사전 완성본 |
| **P3 렌더러** | `ClickableZh`+팝업, **1세트 파일럿→dev 화면 확인**(트렁케이션 주의) | ExamSession.js |
| **P4 수집·관문** | unknownWords 수집 + 회차말 복습 관문 + 누적 단어장 | ClaudeApp.js/진도 스키마 |
| **P5 방지장치** | 미니퀴즈·soft nudge·핵심어·오답리포트·대시보드 지표 | UI |
| **P6 검증·배포** | 전수 검증 + npm build + dev 스팟 + 커밋/푸시(PowerShell) | 배포 |

---

## 10. 리스크와 대응

| 리스크 | 대응 |
|---|---|
| **3,900어 뜻 정확도**(최대 리스크) | 신뢰 시드 재사용 + 기계검증 + 표본 감사 + 정책 B 문맥 오버라이드 |
| 오분사(고유명사·과분할) | 빌드타임이라 감사·수정 가능 + 커스텀사전 + 단자 폴백(클릭불가 0) |
| ExamSession.js 트렁케이션(전례 있음) | 부분편집·백업·esbuild 파싱검증·**1세트 dev 파일럿 먼저** |
| localStorage 비대 | 사전은 번들, 상태만 저장 |
| 기능어(的/了)가 관문 오염 | ~50어 불용어 목록 → 클릭은 되되 수집·마스터리 제외 |
| 모바일 팝업/탭타깃 | 하단 시트 + 충분한 탭 영역, 폰 우선 테스트 |

---

## 11. 승인이 필요한 결정들

1. **사전 범위**: 3,900어 전부(기능어 포함, 단 불용어는 관문 제외) vs 내용어만(~3,000). → 권장: **전부 클릭 가능 + 불용어 관문 제외**.
2. **관문 강제 수위**: soft-block(통과 권장, 건너뛰기 가능) vs hard-block(통과 전 완료 불가). → 권장: **soft-block로 시작**, 데이터 보고 조정.
3. **동의어/반대어 깊이**: 각 1~2개(간결) vs 최대 3개. → 권장: **각 1~2개**(P3 인지부하).
4. **P2 콘텐츠 저작 방식**: 내가 검증기 통과분을 배치 생성 → 세트/빈도 단위로 당신이 표본 확인. (전량 육안은 비현실적)

> 승인해주시면 **P1(분사+사전골격+커버리지 100% 검증)부터** 착수합니다. 여기까지는 콘텐츠 저작 없이 "모든 단어가 빠짐없이 클릭 대상이 되는지"를 기계로 증명하는 단계라 리스크가 낮습니다.
