# 확장 기본 규칙 (학년·시험·과목 확장 시 반드시 준수) — v1

작성: 2026-07-21. 출처: 이 채팅에서 사용자가 확정·강조한 규칙. **향후 P4/P5, 다른 학년(P4~PSLE)·시험(WA/SA/EOY)·과목(영어/수학/과학) 확장 시 처음부터 적용.**

## 1. 언어 규칙 (엄격 — 위반 시 재작업)
- **중국어 시험 화면 = 중국어만.** 문제·지문·보기·어휘함·풀·버튼·라벨·안내문·프롬프트 전부 중국어(`isZh` 게이트).
- **한국어는 앱 어디에도 넣지 않는다.** (보고·소통만 한국어; 학생이 보는 UI엔 금지.)
- **영어는 오직 세 자리에만**: (a) 단어 팝업의 뜻·동의어(Synonym)·반대어(Antonym), (b) 정답/오답 해설, (c) 번역(Translate). 그 외 UI 영어 금지.
- 새 과목/시험도 같은 원칙: 해당 언어 시험은 그 언어로, 영어는 뜻·해설·번역만.

## 2. 단어 팝업 규칙
- **실제 문제 풀이 페이지에 직접 통합.** 별도 페이지/탭 금지.
- **모든 문제유형의 모든 문제·지문·보기·어휘함·풀**에서, 화면에 보이는 모든 (내용어) 중국어 단어를 탭하면 뜻을 볼 수 있어야 함. (선택 동작과 충돌하는 정답칩은 표시전용 박스에서 조회.)
- 팝업 내용: 표제어 + 병음 + 영어 뜻 + 동의어(1~2) + 반대어(1~2) + 예문(중문 + **단어단위 결합 병음** + 영어 번역).
- **분절은 jieba 세그맵 기준**(그리디 longest-match 금지) — 오분사·아티팩트 토큰 금지.
- **뜻 정책 B**: 문맥별 뜻 유지 + 정확성. 예문 병음 단어단위 결합. 뜻은 영어만.
- **커버리지 100%**: 자극 텍스트의 모든 내용어가 클릭 대상. 전수 검증으로 확인.

## 3. 작업 원칙 (사용자 반복 강조)
1. **추측 금지** — 실제 파일 열어 확인 후 판단. 오탐은 정직히 오탐이라 보고.
2. **전수조사** — 샘플 금지. 앱의 실제 파이프라인을 그대로 재현해 전량 검사.
3. **근본 수정** — 증상 땜질 금지(예: 오분사는 세그맵 근본 교체로).
4. **수정 후 검증** — 빌드 통과 + 전수 재검사 + diff 확인.
5. **기존 자산 보존** — 폰트·기존 앱코드·데이터 불필요하게 건드리지 않음.
6. **빌드/푸시** — PowerShell, `npm run build`로 컴파일 확인 후 푸시. 신규 import 파일은 반드시 함께 커밋(안 하면 Vercel 빌드 실패).
7. 보고는 한국어, 간결·정직하게.

## 4. 파이프라인 자산 (재사용)
- 사전: `wa1_zh_dict/zh_dict_authored.json`(마스터), 앱용 `src/data/p3/chinese/zh_vocab.json`, 분절맵 `zh_seg.json`.
- 도구: `author_harness.py`(6-tuple 저작+검증), `worklist.py`, `patch_ex.py`, `build_zh_dict.py`, `seed_py.json`/`seed_en_global.json`(정본 병음/뜻).
- 검증: 앱 segZh 재현 전수 검사기(`audit_*.py`), esbuild JSX 파싱.
- 렌더러: `src/components/ExamSession.js`의 `segZh`·`ClickableZh`·`VocabPopup`.
