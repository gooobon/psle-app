# P4 — 중국어 복습 관문 (Review Gate) 상세 설계 · 구현

작성: 2026-07-21 · Genius Project (psle-app) · 대상: P3 中文 WA1 (향후 학년·과목 확장 시 동일 규칙)

방안 **C (간격 반복 숙달 루프 / spaced mastery loop)** 확정.
제약: **매 회차 ≤ 10 단어**, **무조건 복습 전용 — 새 단어 없음**. 복습 단어는 오직
(1) 학생이 문제를 풀다 **직접 탭한**(=몰랐던) 단어, (2) **틀린 문제의 핵심 단어**에서만 나온다.

---

## 0. 한 줄 요약

> 학생이 "몰라서 눌렀거나 틀린" 단어만 모아서, 시험 세트가 끝난 직후 짧은 인식 퀴즈로
> 확인하게 하고, 못 맞힌 단어는 **다음 회차에 다시** 나오게 한다. 연속으로 맞히면 졸업(마스터).
> 새 단어는 절대 넣지 않는다. 한 회차 최대 10개로 피로를 막는다.

30년차 초등 중국어 교사의 판단: 3학년에게 필요한 건 "많이"가 아니라 **"틀린 그것만, 될 때까지, 부담 없이"**.
그래서 (a) 순수 복습, (b) 10개 상한, (c) 인식→회상 난이도, (d) 간격 반복 졸업 조건을 택했다.

---

## 1. 단어 수집 (무엇을 복습하는가)

두 개의 소스만 사용한다. 둘 다 **학생 자신의 이번 회차 행동**에서 나온다.

| 소스 | 수집 지점 | 코드 |
|---|---|---|
| ① 탭한 단어(몰랐던 단어) | 지문·문제·보기·어휘함·풀 어디서든 단어를 눌러 팝업을 연 순간 | `VocabPopup` → `zhAddUnknown(word)` → `sessionStorage["genius_zh_session_unknown"]` |
| ② 틀린 문제의 핵심 단어 | 세트 종료 시 채점 결과에서 오답 문항의 정답어 + 큐레이션된 keywords | `coreWordsFromWrong(allResults, plan, dict, stop)` |

- **불용어 제외**: 的·了·是·地·得·着 등 기능어는 관문에서 뺀다(`ZH_REVIEW_STOP`). 팝업은 계속 뜨되 복습 대상에서만 제외.
- **사전에 있는 단어만**: `dict[word]`가 없으면 버린다(뜻·예문을 못 보여주므로).
- **중복 제거 + 상한 10**: 합쳐서 dedup 후, 아래 "due" 규칙으로 정렬해 최대 10개만.

MCQ 정답은 인덱스이므로 `options[answer]`로 실제 글자를 뽑고, cloze/match는 `answer`가 곧 단어,
그리고 각 문항의 `keywords[].w`(사람이 검수한 핵심어)를 함께 넣는다.

---

## 2. 간격 반복 저장소 (`src/lib/zhReview.js`)

앱의 진도 구조(`genius_progress_v2`)와 **분리된** 독립 저장소를 쓴다. 학년·과목 확장 시 서로 간섭하지 않게 하기 위함이다.

- localStorage 키: `genius_zh_review_v2`
- 스코프: `window._GENIUS_STORE._currentUser + "_zh"` (계정별 분리, 앱 결합 없음)
- 내부 회차 카운터 `ses`: 관문이 실행될 때마다 +1 (앱의 세션 개념과 독립)

단어별 레코드:
```
{ box, dueSes, sess:[…], att, cor, mastered, firstSes }
```

**Leitner 상자 → 다시 나올 때까지의 회차 간격**
```
box1 → 1회차 후,  box2 → 2,  box3 → 4,  box4 → 8
```

**규칙**
- 처음 수집된 단어: box1, 이번 회차에 바로 due.
- 맞힘: box +1(최대 4), `dueSes = 현재회차 + INTERVAL[box]`, 맞힌 회차를 `sess`에 기록.
- 틀림: box=1로 초기화, `dueSes = 현재회차 + 1` (다음 회차에 반드시 재출제).
- **졸업(마스터)**: 서로 다른 회차에서 **2번 이상** 맞히면 `mastered=true` → 이후 관문에 안 나옴.
- 재수집(또 몰라서 탭)된 단어: due를 앞당겨 이번 회차에 다시 나오게 함.

`assembleGate(collected, dict, limit=10)`:
수집어 등록 → `ses` +1 → `!mastered && dueSes<=현재회차`인 단어를 **가장 밀린 것 우선**으로 정렬 → 상위 10개 반환.

---

## 3. 관문 UI (`src/components/ZhReviewGate.js`)

세트 채점이 끝나면 **성적표(ExamSummary) 직전에** 관문을 띄운다. 통과해야 성적을 본다(soft-hard 중간: 스킵 버튼 없음, 대신 짧고 최대 10문항).

각 단어마다 **인식형 4지선다**:
```
这个词语是什么意思？
   汉字(크게) + 병음
   ○ 영어 뜻 A
   ○ 영어 뜻 B   ← 정답 1개 + 오답 3개(다른 사전 표제어의 뜻에서 자동 생성)
   ○ 영어 뜻 C
   ○ 영어 뜻 D
```
- 오답 선택지는 사전에서 뜻 앞머리(head)가 겹치지 않게 3개 뽑아 섞는다.
- 답을 고르면 즉시 `grade(word, 맞음여부)`로 저장소 갱신.
- **틀리면** 그 자리에서 汉字·병음·뜻·예문(zh/py/en)을 보여주고 "再记一遍" — 회상 전 재노출.
- 마지막에 대시보드:
  ```
  本次复习 X · 答对 Y · 还需巩固 Z
  累计掌握 M 个 · 还在复习 N 个
  ```
  Z>0이면 "다음 회차에 다시 나온다"고 안내 → `onDone()`으로 성적표로.

**언어 규칙(엄수)**: 관문 UI 문구는 전부 중국어. 영어는 **뜻·동의어·예문 번역**에만 (기존 팝업과 동일 정책).
복습할 단어가 하나도 없으면 "本次没有需要复习的词语 🎉" 후 바로 성적표.

---

## 4. 렌더러 연동 (`src/components/ExamSession.js`)

- import: `ZhReviewGate`, zhReview의 `addUnknown/getSessionUnknown/clearSessionUnknown/assembleGate/coreWordsFromWrong`.
- `VocabPopup` 열릴 때 `zhAddUnknown(word)` 호출 (탭=몰랐던 단어 수집).
- `ExamSessionScreen`:
  - state `gateWords`(null=미조립, []=없음, [...]=대상), `gatePassed`.
  - `done && isZh && !reviewMode`일 때 useEffect로 **1회** 조립: 탭어 + 오답 핵심어 → `assembleGate(…,10)` → `clearSessionUnknown()`.
  - 렌더: 중국어 세트이고 아직 통과 전이면 `gateWords`가 있으면 `<ZhReviewGate>`, 없으면 성적표로 통과.
  - `reviewMode`(복습 재풀이) 및 영어 세트에는 **관문 미적용**.

기존 학습층(sentence_en/hints/keywords/evidence/폰트/세그맵)은 **불변**. explanation·채점 로직 무변경.

---

## 5. 향후 학년·과목·시험 확장 규칙 (반드시 승계)

이번 채팅에서 확정된 기본 규칙 — 학년별/시험별/과목별 확장 시 철저히 지킬 것:

1. **추측금지·전수조사·근본수정·수정후검증**: 표본 아닌 전수 검증, 앱 실제 파이프라인을 재현해 감사.
2. **팝업은 실제 시험 풀이 화면에 직접 통합** — 별도 탭·창·데모 페이지 금지. 모든 문제유형의 모든 문제·보기·지문·어휘함·풀·단어에 동일 구조 적용.
3. **언어 규칙**: 중국어 시험 UI는 중국어만. 한국어 금지. 영어는 (a) 단어 뜻/동의어/반의어, (b) 정답·오답 해설, (c) 번역에만.
4. **복습 관문은 순수 복습**: 새 단어 없음. 소스는 탭한 단어 + 틀린 핵심어뿐. 회차 ≤ 10.
5. **불용어는 관문 제외**(팝업은 허용). 사전 미등재어 제외.
6. **저장소 분리·계정 스코프**: 진도 구조와 독립, `_currentUser + "_<subject>"`.
7. **검증 게이트로 품질 강제**: 사람의 성실성 아닌 기계검증으로. esbuild JSX 파싱 통과 필수.
